<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sin este permiso la compilación de publicación no puede hablar con la API. Los
         manifiestos de depuración y de perfil lo declaran por su cuenta, así que el fallo no
         aparece hasta que se instala un APK de publicación. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Para avisar de cada compra detectada y poder apuntarla de un toque. Desde Android 13
         el usuario tiene que concederlo, así que se pide desde la propia aplicación. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Para volver a enganchar la escucha de notificaciones tras un reinicio, sin depender de
         que el usuario se acuerde de abrir la aplicación. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- usesCleartextTraffic: todo va por HTTPS contra api.axchisan.com.
         allowBackup: la app maneja datos económicos y las copias automáticas de Android los
         subirían a Google Drive; el servidor ya es la única fuente de verdad. -->
    <application
        android:label="@string/nombre_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Lee las notificaciones de pago del teléfono para poder registrar las compras
             solas. El permiso lo concede el usuario a mano en Ajustes; declararlo aquí no
             activa nada por su cuenta.

             Va como exported porque quien se conecta al servicio es el propio sistema, desde
             otro proceso. Lo que impide que se conecte cualquier otra aplicación no es ese
             atributo sino android:permission: solo el sistema tiene BIND_NOTIFICATION_-
             LISTENER_SERVICE. -->
        <service
            android:name=".EscuchaDeNotificaciones"
            android:exported="true"
            android:label="@string/nombre_app"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

        <receiver
            android:name=".ReconexionAlArrancar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
